---
paths:
  - "web/**/*"
---

# ⚠️ Next.js 16 is not the Next.js you know

Per `web/AGENTS.md`: this is a newer major with breaking changes to APIs, conventions, and file structure that may differ from training data. **Read the relevant guide in `web/node_modules/next/dist/docs/` before writing web code**, and heed deprecation notices.
